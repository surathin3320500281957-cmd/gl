# Dashboard เงินรับฝาก / เงินจอง / เงินล่วงหน้า

Dashboard สรุปรายการเงินมัดจำ / เงินจอง / เงินล่วงหน้า สำหรับผู้บริหาร เป็นไฟล์ HTML เดี่ยว (single-file) ไม่ต้องมี server หรือฐานข้อมูล ข้อมูลถูกฝังอยู่ในไฟล์ตอนสร้าง

**Live:** โฮสต์อยู่บน GitHub Pages (`surathin3320500281957-cmd.github.io`)

**ไฟล์ปัจจุบัน:** `แดชบอร์ด_เงินมัดจำ_เงินจอง_เงินล่วงหน้า.html` — มี **2 แท็บ**: "แดชบอร์ดหลัก" (เดิม) และ "ข้อมูลต้นฉบับ (ตรวจสอบ)" (เพิ่มใหม่)

---

## ภาพรวมเทคนิค

- **ไม่มี backend/database** — ข้อมูลทั้งหมดถูกเข้ารหัสแบบ dictionary-compressed แล้วฝังใน `<script type="application/json">` หลายก้อนภายในไฟล์ HTML เดียว (`raw-data` สำหรับแท็บหลัก, `verify-data` สำหรับแท็บตรวจสอบ)
- **ไม่มี build tool** ที่ฝั่งผู้ใช้ — เปิดไฟล์ `.html` ในเบราว์เซอร์ได้ทันที
- **ต้องต่ออินเทอร์เน็ต** ตอนเปิดใช้งาน เพราะโหลดฟอนต์ Sarabun จาก Google Fonts และไลบรารี SheetJS (สำหรับ Export Excel) จาก cdnjs
- Stack: Vanilla HTML/CSS/JS ล้วน ไม่มี React/framework
- **Export PDF ทุกจุด** ใช้เทคนิค **Blob URL**: สร้างเอกสาร HTML ใหม่ (เฉพาะตารางที่จะพิมพ์ ไม่มี UI อื่นปน) → ห่อเป็น `Blob` → เปิดแท็บใหม่ด้วย `URL.createObjectURL` → auto-trigger `window.print()` — ข้อดีคือพิมพ์ได้ **ทุกแถวที่กรอง/ค้นหาอยู่** ไม่ใช่แค่หน้าที่มองเห็นบนจอ (ต่างจาก `window.print()` แบบเดิมที่เคยใช้ตอนแรกซึ่งพิมพ์ได้แค่ 50 แถวที่แสดงในตาราง ณ ขณะนั้น)

---

## โครงสร้างไฟล์ Excel ต้นฉบับที่ต้องใช้

ไฟล์ Excel ที่ส่งมาอัปเดตข้อมูลทุกเดือน ต้องมี **2 sheet**:

### 1. Sheet `ONLYDATA` (ใช้โดยแท็บหลัก + เป็น "เดือนใหม่" ของแท็บตรวจสอบ)

โครงสร้างคอลัมน์ (อ้างอิงตำแหน่ง ไม่ใช่ชื่อหัวตาราง):

| ตำแหน่ง (0-indexed) | ชื่อหัวตาราง (ในไฟล์) | ใช้ในงานหรือไม่ |
|---|---|---|
| B (1) | PimaryKEY | ✅ ใช้เชื่อมข้อมูลระดับรายบุคคล (แท็บตรวจสอบ) |
| D (3) | ชื่อGL | ✅ ตัวขับเคลื่อนหลัก |
| E (4) | กอง | ✅ ตัวกรอง |
| F (5) | สคจ./สคล. | ✅ ตัวกรอง |
| H (7) | ชื่อโครงการ | ✅ ตัวกรอง (ค้นหาได้) |
| K (10) | จำนวนเงิน | ✅ ใช้เป็น "เดือนใหม่" ในแท็บตรวจสอบ (ต้นฉบับ ไม่ผ่านสูตร) |
| N (13) | กลุ่มการดำเนินงาน | ✅ ranking chart (แท็บหลัก) |
| O (14) | รายละเอียดดำเนินงาน | ✅ ตารางรายละเอียด (แท็บหลัก) |
| P (15) | ประเภทรายการ | ✅ รายเดิม / รายใหม่ / Clear |
| R (17) | (ในไฟล์ชื่อ "ส่วนต่าง") | ✅ ตัวเลขจำนวนเงินส่วนต่าง |
| S (18) | (ในไฟล์ชื่อ "จำนวนเงินส่วนต่าง") | ✅ ข้อความ เพิ่มขึ้น/ลดลง/เท่าเดิม |
| T (19) | จำนวนเงินสุทธิ | ✅ ยอดหลักของทุกการ์ด/กราฟ (แท็บหลัก) |

> ⚠️ **จุดสำคัญที่ต้องรู้:** หัวตารางคอลัมน์ **R และ S ในไฟล์ต้นฉบับสลับความหมายกัน** (R ชื่อ "ส่วนต่าง" แต่เก็บตัวเลข, S ชื่อ "จำนวนเงินส่วนต่าง" แต่เก็บข้อความ) เป็นความคลาดเคลื่อนจากไฟล์ต้นทางเอง — dashboard ดึงข้อมูลโดยอ้างอิง **ตำแหน่งคอลัมน์** ไม่ใช่ชื่อหัวตาราง จึงไม่ได้รับผลกระทบ แต่ถ้าโครงสร้างไฟล์เปลี่ยนตำแหน่งคอลัมน์ไปจากเดิม ต้องแจ้ง Claude ให้ปรับ mapping ใหม่

### 2. Sheet `BF` (ใช้เป็น "เดือนเก่า" ของแท็บตรวจสอบเท่านั้น)

ไฟล์ก้อนข้อมูลของเดือนก่อนหน้า (snapshot ก่อน seed ข้อมูลใหม่เข้า ONLYDATA) โครงสร้างคอลัมน์คล้าย ONLYDATA แต่ไม่มีคอลัมน์ประเภทรายการ/ส่วนต่าง (เพราะเป็นข้อมูลดิบเดือนเดียว ไม่ใช่ผลเทียบ):

| ตำแหน่ง | ชื่อหัวตาราง | ใช้ |
|---|---|---|
| B (1) | PimaryKEY | ✅ |
| D (3) | ชื่อGL | ✅ |
| E (4) | กอง | ✅ |
| F (5) | สคจ./สคล. | ✅ |
| H (7) | ชื่อโครงการ | ✅ |
| K (10) | จำนวนเงิน | ✅ ใช้เป็น "เดือนเก่า" |

**ที่มาของ "เดือนเก่า vs เดือนใหม่":** เดือนเก่า = ทุกแถวใน sheet `BF` (ไม่กรองอะไรเลย) / เดือนใหม่ = ทุกแถวใน `ONLYDATA` ที่ประเภทรายการ **ไม่ใช่ Clear** (เพราะ Clear = รายการที่ปิด/หายไปแล้ว ไม่มีอยู่ในเดือนใหม่จริง) — ใช้คอลัมน์ **"จำนวนเงิน" ต้นฉบับตรงๆ ไม่ผ่านสูตรใดๆ** ตามที่ผู้ใช้ยืนยัน

---

## กติกาสำคัญที่ต้องคงไว้เสมอ (Business Rules)

เวลาขอแก้ไข/เพิ่มฟีเจอร์ กติกาต่อไปนี้ต้องไม่เปลี่ยน เว้นแต่ตั้งใจแก้โดยเฉพาะ:

### แท็บหลัก
1. **"ส่วนต่าง" มีความหมายเฉพาะกลุ่ม "รายเดิม" เท่านั้น** — แถวที่เป็น รายใหม่ หรือ Clear มีตัวเลขค้างอยู่ในคอลัมน์ R จากไฟล์ต้นฉบับ (ค่าตกค้างจากตอนที่รายการยังไม่ถูกจัดกลุ่ม) — dashboard **บังคับซ่อนเป็น "–" และคิดเป็น 0 เสมอ** ในทุกจุดที่เกี่ยวข้อง: ตารางสรุป, ตารางรายละเอียด, การเรียงลำดับ (sort), และไฟล์ Export
2. **การ์ด "ยอดรวมสุทธิ"** = ผลรวมของ รายเดิม + รายใหม่ เท่านั้น (ไม่รวม Clear)
3. **หลัก cross-filter แบบ "ไม่กรองตัวเอง" (exclude-self)** — การ์ดและกราฟทุกตัวคำนวณค่าจากข้อมูลที่ไม่รวมตัวกรองของตัวเอง เพื่อให้ยังเทียบกับตัวเลือกอื่นได้เสมอแม้กำลังกรองอยู่
4. **breakdown เพิ่มขึ้น/ลดลง/เท่าเดิม** (ในการ์ดรายเดิม) ผูกกับ `card=รายเดิม` เสมอ — สลับไปการ์ดอื่นจะถูกล้างอัตโนมัติ
5. **คอลัมน์ เดือน(เก่า)/จำนวนเงิน(เก่า)/เดือน(ใหม่)/จำนวนเงิน(ใหม่)** ในตารางรายละเอียด: `จำนวนเงิน(ใหม่) = จำนวนเงินสุทธิ` ตรงๆ, `จำนวนเงิน(เก่า) = สุทธิ − ส่วนต่าง` (คนละสูตรกัน ห้ามใช้สูตรเดียวกันทั้งคู่ — เคยแก้บั๊กจุดนี้มาแล้ว) แสดงเฉพาะกลุ่มที่เกี่ยวข้อง: เก่า=รายเดิม+Clear, ใหม่=รายเดิม+รายใหม่
6. **ตัวกรอง "เดือน"** (แท็บหลัก): เดือนเก่า = รายเดิม+Clear, เดือนใหม่ = รายเดิม+รายใหม่ — ทำงานร่วมกับ cross-filter เดิมทั้งระบบ

### แท็บ "ข้อมูลต้นฉบับ (ตรวจสอบ)"
7. **ตารางสรุปตาม GL ใช้ "ราย" แบบนับแถวดิบ** (raw row count จาก BF/ONLYDATA ตรงๆ ไม่ตัดซ้ำตาม PrimaryKEY) — ต่างจากตารางรายละเอียดที่ join/รวมตาม (PrimaryKEY + ชื่อ GL) แล้ว ตัวเลขสองชุดนี้จึงไม่เท่ากันโดยตั้งใจ (ราย รวม = 7,771 เก่า / 7,619 ใหม่ ณ ชุดข้อมูลล่าสุดที่ตรวจแล้ว, ตารางรายละเอียด = 7,165 แถวเพราะ join ซ้ำกันไปแล้ว)
8. **ฟิลเตอร์ ชื่อGL/กอง/สคจ.สคล./โครงการ มีผลกับทั้งการ์ด + ตารางสรุป + ตารางรายละเอียด พร้อมกัน** ส่วนฟิลเตอร์ "เดือน" มีผลกับการ์ด+ตารางรายละเอียดเท่านั้น (ตารางสรุปต้องโชว์เก่า-ใหม่คู่กันเสมอ ไม่ตัดออกฝั่งใดฝั่งหนึ่ง)
9. **การ์ด "เดือนเก่า/เดือนใหม่ (ราย/บาท)"** ผูกกับตัวกรอง "เดือน" ตัวเดียวกับ dropdown (คลิกการ์ด = สลับค่า filter เดือน, คลิกซ้ำ = ล้าง) — ปัจจุบันมีแค่ 4 การ์ดนี้ ผู้ใช้แจ้งว่าจะค่อยๆ เพิ่มการ์ดอื่น (เช่น เพิ่มขึ้น/ลดลง/คงที่) ทีหลัง
10. **ปุ่ม Export Excel/PDF แยกอยู่บนแต่ละตาราง** (ไม่ใช่ปุ่มรวมส่วนกลาง) — ตารางสรุปกับตารางรายละเอียด export แยกไฟล์กัน คนละชุดข้อมูล

---

## ฟีเจอร์ทั้งหมด

### แท็บ "แดชบอร์ดหลัก"
- **การ์ดสรุป 4 ใบ** (กดกรองได้): ยอดรวมสุทธิ / รายเดิม (มี breakdown ย่อยเพิ่มขึ้น-ลดลง-เท่าเดิม กดกรองได้) / รายใหม่ / Clear
- **กราฟ ranking 3 มิติ** (กดแท่งกรองได้): ชื่อ GL, สคจ./สคล., กลุ่มการดำเนินงาน
- **ตัวกรองบนสุด:** กอง, สคจ./สคล. (dropdown), โครงการ (searchable combobox), เดือน (ทั้งหมด/เก่า/ใหม่)
- **Chip แสดงตัวกรองที่ active** พร้อมปุ่มลบทีละอันและปุ่มล้างทั้งหมด
- **ตารางสรุปตามประเภทรายการ**
- **ตารางรายละเอียด:** คอลัมน์ เดือน(เก่า)/จำนวนเงิน(เก่า)/เดือน(ใหม่)/จำนวนเงิน(ใหม่) + ค้นหา/เรียง/แบ่งหน้า (50 แถว/หน้า)
- **Export Excel** และ **Export PDF** (เทคนิค Blob URL — พิมพ์ได้ทุกแถวที่กรองอยู่ ไม่ใช่แค่หน้าที่เห็น)

### แท็บ "ข้อมูลต้นฉบับ (ตรวจสอบ)" — เปรียบเทียบ BF (เดือนเก่า) vs ONLYDATA (เดือนใหม่)
- **ตัวกรอง 5 ตัว:** ชื่อ GL, กอง, สคจ./สคล., โครงการ, เดือน (ทั้งหมด/เก่า/ใหม่) + ปุ่มล้างตัวกรองทั้งหมด + chip แสดงตัวกรอง active
- **การ์ด 4 ใบ** (คลิกกรองได้ ผูกกับตัวกรองเดือน): เดือนเก่า·ราย / เดือนเก่า·บาท / เดือนใหม่·ราย / เดือนใหม่·บาท
- **ตารางสรุปตาม ชื่อ GL:** จัดกลุ่มตาม GL แล้วแตกย่อยเป็นโครงการ (ใช้ rowspan) แต่ละแถวมีคอลัมน์ เดือนเก่า(ราย,เงิน) / เดือนใหม่(ราย,เงิน) / ส่วนต่าง(ราย,เงิน) พร้อมแถวสรุปย่อยท้ายกลุ่ม GL และแถวรวมทั้งหมดล่างสุด — มีปุ่ม Export Excel/PDF ของตัวเอง
- **ตารางรายละเอียด:** ระดับ PrimaryKEY + ชื่อ GL (join เก่า-ใหม่เข้าด้วยกัน) คอลัมน์ PimaryKEY/ชื่อGL/กอง/สคจ.สคล./โครงการ/จำนวนเงิน(เก่า)/จำนวนเงิน(ใหม่)/เพิ่มลด ค้นหา/เรียง/แบ่งหน้าได้ — มีปุ่ม Export Excel/PDF ของตัวเอง (แยกจากตารางสรุป)

---

## วิธีอัปเดตข้อมูลเดือนใหม่

1. เปิดแชทกับ Claude (แชทเดิมหรือแชทใหม่ก็ได้ — แนบ README นี้ให้ Claude อ่านก่อนถ้าเป็นแชทใหม่)
2. แนบไฟล์ Excel ที่มี sheet `ONLYDATA` (โครงสร้างคอลัมน์เดิม) **และ** sheet `BF` (ข้อมูลดิบเดือนก่อนหน้า สำหรับแท็บตรวจสอบ)
3. พิมพ์บอกว่าต้องการอัปเดตข้อมูล dashboard
4. Claude จะตรวจสอบโครงสร้างคอลัมน์ให้ตรงกับที่ใช้อยู่ก่อนเสมอ แล้วสร้างไฟล์ HTML ใหม่ (ใช้โค้ด/ดีไซน์/ฟีเจอร์เดิมทั้งหมด เปลี่ยนแค่ข้อมูล)
5. นำไฟล์ใหม่ไปแทนที่ไฟล์เดิมบน GitHub Pages

> ไฟล์นี้เป็น **static file** — ไม่ได้ต่อฐานข้อมูลสด ต้องขอไฟล์ใหม่จาก Claude ทุกครั้งที่มีข้อมูลเดือนใหม่ (ไม่สามารถอัปเดตข้อมูลจากในเบราว์เซอร์ได้เอง)

### เริ่มแชทใหม่กับ Claude
พิมพ์ทำนองนี้: *"ต่อจากที่คุยเรื่อง dashboard เงินมัดจำ เงินจอง เงินล่วงหน้า อยากจะอัปเดต/แก้ไข..."* — Claude สามารถค้นประวัติแชทเก่าเพื่อดึงบริบทได้เอง หรือจะแนบไฟล์ README.md นี้พร้อมไฟล์ HTML เวอร์ชันปัจจุบันให้ Claude อ่านก่อนเริ่มงานก็ได้เช่นกัน (แนะนำแบบหลังถ้าอยากให้ชัวร์ที่สุด เพราะการค้นประวัติแชทอาจตกหล่นรายละเอียดปลีกย่อยได้)

---

## คำแนะนำเรื่องความเสี่ยง / การเก็บเวอร์ชัน

แนะนำให้ **เก็บไฟล์เวอร์ชันก่อนหน้าไว้เสมอ** ก่อน deploy ทับ เช่นตั้งชื่อ `dashboard_2569-07.html`, `dashboard_2569-08.html` ตามเดือนที่อัปเดต หากเวอร์ชันใหม่มีปัญหาที่ยังไม่ทันตรวจเจอ จะได้สลับกลับไปใช้ไฟล์เก่าได้ทันทีโดยไม่ต้องรอแก้ไข

---

## ประวัติการพัฒนา (Changelog)

| รอบ | สิ่งที่ทำ |
|---|---|
| 1 | โครงสร้างพื้นฐาน: 4 การ์ด, 3 กราฟ ranking, ตารางสรุป/รายละเอียด, Export Excel/PDF (แบบ `window.print()` ธรรมดา) |
| 2 | ปรับโทนสีให้สว่าง/สดใสขึ้น, เอาคำว่า "ขั้นที่ 1/2/3" ออกจากหัวข้อ |
| 3 | แก้ label ตัวเลขบนกราฟให้อ่านง่ายขึ้น (ใส่พื้นหลังขาวทึบกันสีตัวอักษรจมกับแท่งกราฟ) |
| 4 | เพิ่มช่องค้นหาโครงการ (searchable combobox), ปรับชื่อ Dashboard ให้เด่นขึ้น |
| 5 | แก้บั๊ก: กล่องค้นหาโครงการไม่ปิดหลังเลือก (สาเหตุ: CSS specificity ทับ attribute `hidden`) |
| 6 | การ์ดสรุปกดกรองได้ (คลิกเพื่อกรองทั้งหน้า, คลิกซ้ำยกเลิก) |
| 7 | เพิ่ม breakdown เพิ่มขึ้น/ลดลง/เท่าเดิม ในการ์ดรายเดิม |
| 8 | แก้ค่าส่วนต่างตกค้างของ Clear/รายใหม่ ให้ซ่อนเป็น "–" ทุกจุด (ตาราง/sort/export) |
| 9 | ทดลอง export PDF แบบ jsPDF + ฝังฟอนต์ไทย (Blob จริง ไม่ใช่ print) — **ยกเลิก** เพราะซับซ้อน/เสี่ยงเกินไปเทียบกับที่ได้ (ไฟล์ 9,236 แถว = 319 หน้า 25MB) |
| 10 | เปลี่ยน Export PDF เป็นเทคนิค **Blob URL + เปิดแท็บใหม่ + auto print()** (ผู้ใช้ให้โค้ดมาจาก AI ตัวอื่น Claude รับช่วงต่อ) — แก้ปัญหาเดิมที่พิมพ์ได้แค่ 50 แถวที่เห็นบนจอ ตอนนี้พิมพ์ได้ทุกแถวที่กรองอยู่ |
| 11 | เพิ่ม 4 คอลัมน์ในตารางรายละเอียด: เดือน(เก่า)/จำนวนเงิน(เก่า)/เดือน(ใหม่)/จำนวนเงิน(ใหม่) — แก้บั๊กสูตรรอบแรก (ใช้สุทธิ−ส่วนต่างทั้งคู่ผิด) เป็น ใหม่=สุทธิตรงๆ, เก่า=สุทธิ−ส่วนต่าง |
| 12 | เพิ่มตัวกรอง "เดือน" (ทั้งหมด/เดือนเก่า/เดือนใหม่) ในแท็บหลัก ผูกกับ cross-filter เดิมทั้งระบบ |
| 13 | **เพิ่มแท็บใหม่ "ข้อมูลต้นฉบับ (ตรวจสอบ)"** — เปรียบเทียบ sheet BF (เดือนเก่า) กับ ONLYDATA (เดือนใหม่) ที่ระดับ PrimaryKEY+ชื่อGL เวอร์ชันแรก: ตารางสรุป (gl+proj) + ตารางรายละเอียด แบบ flat ไม่มีการ์ด/ฟิลเตอร์เพิ่มเติม |
| 14 | ปรับตารางสรุปแท็บตรวจสอบใหม่ทั้งหมด: column เดือนเก่า(ราย,เงิน)/เดือนใหม่(ราย,เงิน)/ส่วนต่าง(ราย,เงิน), row จัดกลุ่มตาม ชื่อ GL (rowspan) แตกย่อยโครงการ พร้อม subtotal — เพิ่มตัวกรองเดือน + Export Excel/PDF ของแท็บตรวจสอบเอง |
| 15 | เพิ่มการ์ด + ฟิลเตอร์ (ชื่อGL/กอง/สคจ.สคล./โครงการ) ให้แท็บตรวจสอบ — เวอร์ชันแรกเป็นการ์ด รวม/เพิ่มขึ้น/ลดลง/คงที่ (คำนวณระดับ PrimaryKEY) |
| 16 | ปรับการ์ดแท็บตรวจสอบใหม่ตามที่ขอ: เหลือ 4 ใบง่ายๆ — เดือนเก่า(ราย/บาท) + เดือนใหม่(ราย/บาท) ผูกกับตัวกรองเดือน (การ์ดแบบเดิม up/down/same เก็บไว้เพิ่มทีหลัง) |
| 17 | ย้ายปุ่ม Export Excel/PDF ของแท็บตรวจสอบ จากส่วนกลาง (บนสุด) ไปไว้บนแต่ละตารางแยกกัน (ตารางสรุป vs ตารางรายละเอียด export คนละไฟล์) |

**ตัวเลขอ้างอิงล่าสุด (ชุดข้อมูลที่ตรวจสอบผ่านแล้ว):**
- แท็บหลัก (ONLYDATA): รายเดิม 7,022 / รายใหม่ 597 / Clear 765 = รวม 8,384 รายการ
- แท็บตรวจสอบ: BF (เดือนเก่า) 7,771 แถวดิบ / ONLYDATA ไม่รวม Clear (เดือนใหม่) 7,619 แถวดิบ — ตรงกับตัวเลขที่ผู้ใช้ยืนยันจากภาพต้นฉบับ
- ยอดเงินรวม BF: 88,640,274.34 บาท / ยอดเงินรวม ONLYDATA (ไม่รวม Clear): 19,346,509.23 บาท

---

## ข้อจำกัดที่ควรรู้

- ต้องเปิดผ่านอินเทอร์เน็ต (โหลดฟอนต์ + ไลบรารี Export Excel จาก CDN)
- ไม่รองรับการแก้ไขข้อมูลย้อนหลังจากในตัว dashboard เอง — ต้องแก้ที่ไฟล์ Excel ต้นทางแล้วขอไฟล์ใหม่
- จำนวนข้อมูลรองรับได้สบายในระดับหมื่นแถว หากข้อมูลเพิ่มมากกว่านี้หลายเท่า ควรแจ้ง Claude เพื่อพิจารณาปรับโครงสร้างการโหลดข้อมูล
- **แท็บตรวจสอบต้องมี sheet `BF` แนบมาด้วยเสมอ** ถ้าไฟล์ Excel ที่ส่งมามีแค่ `ONLYDATA` (ไม่มี `BF`) แท็บตรวจสอบจะใช้ข้อมูลเดือนเก่าเดิมค้างอยู่ (ไม่อัปเดต) — ต้องแจ้ง Claude ให้ชัดเจนว่าจะข้ามการอัปเดตแท็บตรวจสอบรอบนี้หรือไม่ ถ้าไม่มีไฟล์ BF ใหม่
